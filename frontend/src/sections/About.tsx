import { aboutContent } from "../config/config"
import SectionLayout from "../layouts/SectionLayout"

const About = () => {
    return (
        <SectionLayout title={aboutContent.title}>
            a
        </SectionLayout>
    )
}

export default About